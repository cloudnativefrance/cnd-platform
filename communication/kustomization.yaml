apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Explicit children — keeps cnd-communication scoped to matrix + mattermost.
# The photos/ subdir lives here for taxonomy but is reconciled by its own
# Flux Kustomization (cnd-photos), not by cnd-communication.
resources:
  - matrix
